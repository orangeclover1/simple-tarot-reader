[project]
name = "lantern-tarot"
version = "0.1.0"
description = "A private, offline, text-first tarot journal."
requires-python = ">=3.11"
dependencies = ["kivy==2.3.1"]

[tool.ruff]
line-length = 100
